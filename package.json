{
  "name": "blogpost-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "BACKEND",
    "FRONTEND/**"
  ],
  "scripts": {
    "prepare": "node -e \"process.env.NODE_ENV !== 'production' && require('child_process').exec('husky')\"",
    "lint-staged": "lint-staged"
  },
  "keywords": [],
  "author": "Ignaccio7",
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",    
    "zod": "3.22.4"
  }
}
